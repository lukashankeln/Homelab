apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kagent
  namespace: kagent
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: cilium
      sectionName: kagent
  hostnames:
    - kagent.hankeln.work
  rules:
    - backendRefs:
        - name: kagent-ui
          port: 8080
